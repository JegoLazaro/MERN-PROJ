import { defineConfig } from 'vite';
import tailwindcss from '@tailwindcss/vite';
// other imports like 'react()' or 'vue()'

export default defineConfig({
  plugins: [
    // other plugins
    tailwindcss(),
  ],
});
